# ────────────────────────────────────────────────
# PyTorch (CUDA 12.8) — Docker 안에서는 아래 커맨드로 설치
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# ────────────────────────────────────────────────

# Core (torch/torchvision은 Dockerfile에서 CUDA 빌드로 설치)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# Dataset
pycocotools>=2.0.7
Pillow>=10.0.0

# Config
ConfigArgParse>=1.7

# Tracking
tensorboard>=2.15.0

# Visualization
opencv-python>=4.8.0

# Utils
numpy>=1.26.0
tqdm>=4.66.0
scipy>=1.11.0
